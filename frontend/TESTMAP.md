# テストマップ：Firebaseログイン実装

## 1. 正常系テスト

- [ ] 有効なメールアドレスとパスワードでログインできる
- [ ] ログイン成功後、ダッシュボード（またはトップ）へ遷移する

## 2. 異常系テスト

- [ ] 間違ったパスワードを入力した場合にエラーメッセージが表示される
- [ ] 登録されていないメールアドレスでエラーメッセージが表示される
- [ ] 空の状態でボタンを押しても送信されない

## 3. 実装確認（ToDo）

- [x] Firebaseプロジェクトの作成と設定
- [x] .env.local へのAPIキー設定
- [x] firebase.ts の作成（初期化）
- [ ] ログイン画面（UI）の作成
- [ ] ログイン処理（Firebase Auth）の紐付け

# 動作確認テスト

## 1. 認証機能 (Firebase Auth)

- [ ] 未登録のメールアドレスでログインを試み、エラーメッセージが表示されるか
- [ ] 登録済みのメールアドレスと間違ったパスワードで、エラーが表示されるか
- [ ] 正しいメールアドレスとパスワードで、トップページへ遷移するか

## 2. 基本実装 (React基礎)

- [ ] 練習ページで文字を入力した際、リアルタイムで下のテキストが更新されるか
- [ ] ブラウザの拡張機能による「Hydration Error」が消えているか

# 管理画面の守備

- [ ] Middlewareの作動確認：ブラウザのクッキーを削除した状態で /admin にアクセスした際、即座に /login へリダイレクトされるか（「チラ見え」せずに追い返されるか）

## 認証ガードの確認

- [ ] ログアウト状態で管理画面のURL（/practiceなど）を直接入力したとき、正しく追い返されるか（404またはログインへ）
- [ ] ログイン状態のときだけ、ページの内容が正常に表示されるか
- [ ] ログイン後、ページをリロードしても「追い返されず」にそのまま表示されるか

## 共通部品

- [ ] ボタンや入力欄を「部品（Component）」として切り出し、他の場所から呼び出せるか
- [ ] 部品を共通化しても、`useState` による文字入力が正しく動作するか
- [ ] Props（プロップス）の受け渡し：共通化した入力部品に、外から label（「メールアドレス」など）を渡して正しく表示されるか

## 1. 門番（Middleware）の鉄壁チェック

- [ ] ログインしていない時、/admin にアクセスすると「一瞬も表示されず」に /login へ飛ばされるか
- [ ] 手動でクッキーを消した際、即座に守備が有効になるか

## 2. 共通部品（UIコンポーネント）の品質

- [ ] 作成した `AromaInput` 部品が、別のページ（例：ログイン画面とプロフィール編集画面）の両方で正しく表示されるか
- [ ] 部品側の `onChange` が呼び出し元の `useState` を正しく更新できているか

## 実装確認

- [ ] AuthとMiddlewareの連携：ログイン成功時に「ログイン済みの印（クッキー）」を発行する処理の実装
